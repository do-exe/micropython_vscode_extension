# MicroPython Extension

Documentation update soon.

This repo is being cleaned and restarted around a new embedded framework direction for MicroPython, Arduino, ESP-IDF, STM32, and other microcontroller platforms.

Driver xAI has been downgraded from the active extension surface for now. It is parked during this cleanup so the core programming, flashing, toolchain, device, and MCP workflows can mature first.
