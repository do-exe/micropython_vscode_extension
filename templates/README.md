# Templates

Reusable project skeletons live here by platform/runtime. Templates describe the file structure used to create applications; real applications live under `workspace/`.

```text
templates/stm/
templates/micropython/
templates/raspberry-pi/
```

Copy a template into `workspace/` before editing it for a real device/project.
